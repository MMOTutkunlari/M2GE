Metin2'de Syserr Çeşitleri


Server tabanlı
Syserr => hataları gösterir.
Syslog => İşlem kayıtlarını tutar.

Auth => kullanıcı giriş işlemleri
Channals => Kanallar
DB => database


Client Tabanlı;
Client tabanlı syserr'de sadece istemci ile alakalı sorunlar yazılır. Oyunun klasörü içinde syserr.txt dosyasında bulur.

-----------------------------------------------------------------
Metin2 quest dosyaları derlendiği zaman object klasöründe bulunur.
3 ayrı dosyaya parçalanır.
.arg => when satırında yazılı olan
.script => when blogunun içinde yazılı olan
.when => getqf bilgilerini tutar.
-------------------------------------------------
when çeşitleri
click (x) => npcye tıklandığında hiçbir chat penceresi gelmeden işlem yaptırır.
kill (x) => bir slotu öldürdüğünüzde çalışacak olan.
timer (x) => zamanlayıcı
levelup (x) => level aldığınızda olacaklar.
login (x) => oyuna girişte yazılaaklar.
logout (x) => oyuna çıkışta yazılacaklar.
button (x) => lettera tıkladığımızda yapılacaklar.
info (x) => lettera tıkladığımızda yapılacaklar.
chat (x) => NPC'ye tıkladığımızda konuşma seçeneklerini oluşturur.
in (-) => bilmiyorum.
out (-) => bilmiyorum.
use (x) => İteme sağ tıkladığımızda yapılacaklar.
server_timer (x) => zamanlayıcı.
enter (-) => bilmiyorum.
leave (-) => bilmiyorum.
letter (x) => sol tarafta iconların çıkmasını sağlar.
take (x) => eşyayı npcye sürüklediğimizde yapılacaklar.
target (x) => NPC başında çıkan sarı ok.
party_kill (x) => parti ile yapılacak işlemler (at zırhlama görevi.)
unmount (-) => Binekten inmek.
pick (-) => kazı işlemi
sig_use (x) => birden fazla item aynı işlemi yapılacaksa special_item_group'a grup oluşturulur onun adı verilir.
item_informer (-) => eşya özelliklerini sıralar.
die (x) => Karakteriniz ölünce yapılacaklar.

------------------------------------------
1. anlatım: letter, button or info.
send_letter(string) => Sol taraftaki iconun altında yazan isim. (string değer alır.)
say_title(string) => Quest yazı penceresinde sarı yazar. Başlıklarda kullanılır.
say_reward(string) => turuncu yazmasını sağlar.
string.format() => değerli yazılarda kullanılır.

-----------------------------------
2. anlatım: login ve logout ve levelup
notice(string) => GM'lerin yazdığı gibi uyarı çıkarır sadece oyuncu kendisine gözükür.
notice_all(string) => GM'lerin yazdığı gibi uyarı çıkarır tüm oyuncular görür.
pc.get_name() => Karakterin adını alır.
pc.get_level() => karakterin levelini yazar.

----------------------------------------------
3. anlatım: chat, click, target
mob_name(int) => İnt değerine mob vnumunu yazdığınız zaman mob adını yansıtır.
find_npc_by_vnum(int) => Haritada en yakın noktadaki npcnin kordinatlarını alır.
target.delete() => NPC'nin üstündeki sarı oku silmek için.

4. anlatım: kill, die, party_kill
syschat(string) => chat menüsüne veri girer.

5. anlatım: use, sig_use
-----------------------
6. anlatım: take
item. npcye sürüklenen itemin bilgilerini alır.
item.get_name() => npcye sürüklenen itemin adını verir.

------------------------
7. anlatım: timer
loop_timer(string, boolean) => timer başlatır.
---------------------------------
8. anlatım: server_timer
clear_server_timer(string) => Hali hazırda çalışan server timerini kapatmak için kullanılır.
server_loop_timer(string, boolean) => Server timeri atar.

-----------------------------------------------------
1. Anlatım: pc fonksiyonları;
PC.Get fonksiyonları.
pc.get_account => string değer dönderir. Hesap adını verir.
pc.get_account_id => int değer döndürür. Hesap idsini.
pc.get_alignment => int değer döndürür. Kahramanlık derecesi.
pc.get_dx => int değer döndürür. Savunmamuzu
pc.get_empire => int değer döndürür. Krallığı döndürür. (1=> kırmızı, 2=> Sarı, 3=> Mavi)
pc.get_empty_inventory_count => int değer döndürür. Envanterdaki boş olan kutuların sayısını.
pc.get_equip_refine_level => int değer döndürür. +oranını
pc.get_exp => int değer döndürür. Karakterdeki exp miktarı.
pc.get_exp_level
pc.get_exp_level0
pc.get_gm_level => int değer döndürür. Karakterin gm seviyesi.
pc.get_gold => int değer döndürür. Yang miktarını verir.
pc.get_guild => int değer döndürür. Lonca varsa idsini verir yoksa 0 döndürür.
pc.get_horse_level => int değer döndürür. At levelini verir.
pc.get_hp => İnt. Hayat Puanı değerini döndürür.
pc.get_ht
pc.get_informer_item
pc.get_informer_type
pc.get_ip0
pc.get_iq
pc.get_job => int değer döndürür. Karakterin cinsiyetini verir.
pc.get_killee_drop_pct 
pc.get_language
pc.get_language2
pc.get_leadership
pc.get_level => İnt değer döndürür. Karakterin levelini verir.
pc.get_local_x => İnt değer dönüdür. Karakterin bulunduğu konumu verir. 
pc.get_local_y => İnt değer döndürür. Karakterin bulunduğu konumu verir.
pc.get_logoff_interval
pc.get_map_index => İnt değer döndürür. Karakterin bulunduğu haritanın idsini verir.
pc.get_max_hp => int değer döndürür. Karakterin alabileceği maxsimum hayat puanı.
pc.get_max_sp =>  int değer döndürür. Karakterin alabileceği maxsimum hayat puanı.
pc.get_money => int değer döndürür.
pc.get_name => string değer döndürür. Karakterinizin adını verir.
pc.get_next_exp => int değer döndürür. Bir sonraki levele ulaşabilmek için gerekli exp miktarını verir.
pc.get_part
pc.get_player_id => int değer döndürür. Karakterin idsini verir.
pc.get_playtime => int değer döndürür. Karakterin toplam oyun saatini verir.
pc.get_premium_remain_sec => int değer döndürür. Premium abonelik süresini verir.
pc.get_race => int değer döndürür. Karakter sınıfını verir.
pc.get_real_alignment => int değer döndürür. Sıralama puanı.
pc.get_sex => int, cinsiyet
pc.get_sig_items => int, gruplandırılmış itemleri verir.
pc.get_skill_group => Karakterin yetenek grubunu verir. (Bedensel - Zihinsel)
pc.get_skill_level => int arg alır, int değer döndürür. Yetenek levelini verir.
pc.get_skill_point => int değer döndürür. Yetenek puanlarını verir.
pc.get_socket_items => int - dizi, karakterdeki itemlerin socketlerini verir.
pc.get_sp => int, mana puanını verir.
pc.get_special_ride_vnum
pc.get_st
pc.get_start_location
pc.get_vid => karakterin auth idsi.
pc.get_war_map
pc.get_weapon => int değer döndürür. Karakterin elinde olan silahın idsini verir.
pc.get_wear => int değer döndürür. İdsini verdiğimiz itemin wear flaglarını döndürür.
pc.get_x => int değer döndürür. Karakterin bulunduğu konumun x değerini verir.
pc.get_y => int değer döndürür. Karakterin bulunduğu konumun y değerini verir.
pc.getarmor => int değer döndürür. Elbisesnini idsini verir.
pc.getcheque
pc.getcurrentmapindex => int değer döndürür. Bulunduğu haritanın idsini verir.
pc.getempire => int değer döndürür. Krallığı verir. (1 => kırmızı, 2 => sarı, 3 => mavi)
pc.getf => 2 string argumanı var. 1. argumant => quest adı. 2 argument => değer. int değer döndürür. Argument ile belirlenen değişkenin valuesini verir.
pc.getgold => int değer döndürür. Karakterin yang miktarını verir.
pc.getguild => int değer döndürür. Karakterin lonca idsini verir.
pc.gethp => int değer döndürür. Karakterin hayat puanını verir.
pc.getleadership
pc.getmaxhp => int değer dönüdrür. Karakterin maks hayat puanını verir.
pc.getmaxsp => int değer döndürür. Karakterin maks manasını verir.
pc.getmoney => int değer döndürür. Karakterin Yang miktarını verir.
pc.getname => string deeğr döndürür. Karakterin adını verir.
pc.getplaytime => int değer döndürür. Karakterin toplam oyunda geçirdiği zamanı verir.
pc.getqf => 1 argument alır. QF değeri verirlir. İnt döndürür. Değerin valuesini döndürür.
pc.getsp => int değer döndürür. Karkaterin mana puanını verir.
pc.getweapon => int değer döndürür. Karakterin elindeki eşyanın idsini verir.
pc.getx => int değer döndürür. Karakterin bulunduğu konumun x değerini verir.
pc.gety => int değer döndürür. Karakterin bulundupu konumun y değerini verir.

2. Anlatı, item fonksiyonları;
item.add_attr0
item.can_over9refine
item.change_attr0
item.change_to_over9
item.clear_attr0
item.copy_and_give_before_remove
item.count_attr0
item.get_addon_type
item.get_antiflag0
item.get_apply0
item.get_attr0
item.get_attribute_set_index
item.get_cell
item.get_count
item.get_id
item.get_immuneflag0
item.get_level
item.get_level_limit
item.get_name
item.get_over9_material_vnum
item.get_refine_vnum
item.get_seal_date
item.get_shop_cheque
item.get_shop_price
item.get_size
item.get_socket
item.get_sub_type
item.get_type
item.get_value
item.get_vnum
item.get_wearflag0
item.has_antiflag0
item.has_flag
item.has_immuneflag0
item.has_wearflag0
item.is_available0
item.is_sealed
item.next_refine_vnum
item.over9refine
item.remove
item.select
item.select_cell
item.set_attr0
item.set_count0
item.set_seal_date
item.set_shop_cheque
item.set_shop_price
item.set_socket
item.set_value
item.start_realtime_expire